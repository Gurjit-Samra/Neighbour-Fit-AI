kind = "web"
previewPath = "/"
title = "NeighbourFit AI"
version = "1.0.0"
id = "artifacts/neighbourfit"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20938

[services.development]
run = "pnpm --filter @workspace/neighbourfit run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/neighbourfit", "run", "build" ]
publicDir = "artifacts/neighbourfit/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20938"
BASE_PATH = "/"
